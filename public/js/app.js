(function() {
	'use strict';

	var meanCrudApp = angular.module('meanCrudApp', ['ui.router']);

/*
		meanCrudApp.config(['$stateProvider', '$urlRouterProvider', configRoutes]);

	function configRoutes($stateProvider, $urlRouterProvider){
		$stateProvider
			.state('home', {
				url: '/',
				templateUrl: 'views/home.html'
			})
			.state('employees',{
				url: '/employees',
				controller: 'employeeController',
				//controllerAs: 'vm',
				templateUrl: 'views/employees.html'
			});

		$urlRouterProvider.otherwise('/');
	}
*/
})();